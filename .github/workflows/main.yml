# main.yml
on: [push]

jobs:
  operator:
    runs-on: ubuntu-latest
    name: build operator
    steps:
    - uses: actions/checkout@v3
      with:
        submodules: recursive
    - run: ls -lah
    - run: ls -lah ./execDAT-operator
    - uses: ./execDAT-operator/.github
